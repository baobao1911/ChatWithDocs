apiVersion: v1
kind: PersistentVolume
metadata:
  name: model-serving-pv-volume
  namespace: model-serving
  labels:
    type: local
spec:
  storageClassName: hostpath
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data"
